export const ALGORITHMS = [
  {
    id: "bubbleSort",
    name: "Bubble Sort",
    category: "Sorting",
    pseudocode: [
      "function bubbleSort(array, n):",
      "    for i from 0 to n - 2:",
      "        swapped = false",
      "        for j from 0 to n - i - 2:",
      "            if array[j] > array[j + 1]:",
      "                swap(array[j], array[j + 1])",
      "                swapped = true",
      "            end if",
      "        end for",
      "        if swapped == false:",
      "            break",
      "        end if",
      "    end for",
      "end function",
    ],
    complexity: {
      best: "O(n)",
      average: "O(n²)",
      worst: "O(n²)",
      space: "O(1)",
    },
    notes: "Stable sort, easy to visualize",
  },
  {
    id: "mergeSort",
    name: "Merge Sort",
    category: "Sorting",
    pseudocode: [
      "function mergeSort(array, left, right):",
      "    if left < right:",
      "        mid = floor((left + right) / 2)",
      "        mergeSort(array, left, mid)",
      "        mergeSort(array, mid + 1, right)",
      "        merge(array, left, mid, right)",
      "    end if",
      "end function",

      "function merge(array, left, mid, right):",
      "    n1 = mid - left + 1",
      "    n2 = right - mid",
      "    create arrays L[0..n1-1] and R[0..n2-1]",
      "    for i from 0 to n1 - 1:",
      "        L[i] = array[left + i]",
      "    end for",
      "    for j from 0 to n2 - 1:",
      "        R[j] = array[mid + 1 + j]",
      "    end for",
      "    i = 0, j = 0, k = left",
      "    while i < n1 and j < n2:",
      "        if L[i] <= R[j]:",
      "            array[k] = L[i]",
      "            i = i + 1",
      "        else:",
      "            array[k] = R[j]",
      "            j = j + 1",
      "        end if",
      "        k = k + 1",
      "    end while",
      "    while i < n1:",
      "        array[k] = L[i]",
      "        i = i + 1",
      "        k = k + 1",
      "    end while",
      "    while j < n2:",
      "        array[k] = R[j]",
      "        j = j + 1",
      "        k = k + 1",
      "    end while",
      "end function",
    ],
    complexity: {
      best: "O(n log n)",
      average: "O(n log n)",
      worst: "O(n log n)",
      space: "O(n)",
    },
    notes: "Divide and conquer; good next step after bubble",
  },
  {
    id: "binarySearch",
    name: "Binary Search",
    category: "Searching",
    pseudocode: [
      "function binarySearch(array, target, left, right):",
      "    while left <= right:",
      "        mid = floor((left + right) / 2)",
      "        if array[mid] == target:",
      "            return mid",
      "        else if array[mid] < target:",
      "            left = mid + 1",
      "        else:",
      "            right = mid - 1",
      "        end if",
      "    end while",
      "    return -1  # target not found",
      "end function",
    ],
    complexity: {
      best: "O(1)",
      average: "O(log n)",
      worst: "O(log n)",
      space: "O(1)",
    },
    notes: "Requires sorted array",
  },
  {
    id: "linearSearch",
    name: "Linear Search",
    category: "Searching",
    pseudocode: [
      "function linearSearch(array, target, n):",
      "    for i from 0 to n - 1:",
      "        if array[i] == target:",
      "            return i",
      "        end if",
      "    end for",
      "    return -1  # target not found",
      "end function",
    ],
    complexity: { best: "O(1)", average: "O(n)", worst: "O(n)", space: "O(1)" },
    notes: "Simple sequential scan",
  },
];
